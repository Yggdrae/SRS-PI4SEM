/* import { Funcionario } from "../../domain/entity/Funcionario";

export interface IGestaoFuncionarioGateway {
	obterFuncionarioPeloId(id: number): Promise<Funcionario>;

	//obterIdFuncionarioPelaMatricula(matricula: string): Promise<number>;
}
 */